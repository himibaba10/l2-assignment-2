# l2-assignment-2

To start working with the project, we first need to write:
npm install

That will install the dependencies.
And then we have to add the environment variables

Then we start the project by npm run start:dev
